# Evidence

- User command failed with `sarpyx: error: unrecognized arguments: pipeline s1_tops`.
- `/Users/roberto.delprete/.local/bin/sarpyx --help` showed an older CLI without
  the `pipeline` subcommand.
- `python -m sarpyx.cli.main pipeline --list` from the repo listed built-in
  pipelines successfully.
