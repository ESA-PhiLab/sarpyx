# Next

- Use `make -f Makefilev2 pipeline-sen /lustre/projects/1001/rdelprete/WORLDSAR-v2/product_list.txt batch_size=6 poll_interval=60` when ready to launch the rolling production run.
- Avoid mixing old-style direct-GPT jobs with the new wrapper if operational isolation is required.
