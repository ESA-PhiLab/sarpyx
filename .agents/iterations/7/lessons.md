# Lessons

Validate BEAM-DIMAP `.hdr` and `.img` payloads before launching SNAP when creating synthetic redirect products; otherwise SNAP reports late Java raster errors instead of the actionable missing file. For operational runs on Lustre, rebuild once with copied payloads after a subap TC failure before giving up.
