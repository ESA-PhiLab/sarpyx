# Next

- Run `make -f Makefilev2 setup-workers` on SpaceHPC when ready to create the actual worker envs and SNAP dirs.
- Submit one product with `WORKER_ID=0` to validate the worker path before using `pipeline-sen`.
