# Evidence

- `sarpyx/snapflow/op.py` declares `snap_operators` with 217 SNAP operator names.
- The file only exposes `generate_operators_help()`; arbitrary execution needs a generic validated GPT command wrapper.
- Prior lesson: keep skill routing tied to current explicit command surfaces.
