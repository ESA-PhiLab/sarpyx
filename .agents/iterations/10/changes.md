# Changes

- Bumped `pyproject.toml` project version from `1.0.0` to `1.0.1`.
- Updated `sarpyx.__version__` resolution to prefer local `pyproject.toml` when present, then fall back to installed package metadata.
