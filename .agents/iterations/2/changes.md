# Changes

- Updated `environment.yml` to install `-e .[copernicus]`.
- Changed `scripts/install_conda.sh` to create/update from `environment.yml`.
- Added post-install verification for `gpt`, `sarpyx`, and `phidown`.
- Updated Makefile docs and tests for the install contract.
